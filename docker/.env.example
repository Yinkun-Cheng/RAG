# PostgreSQL
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_password
POSTGRES_DB=rag_db

# Backend
BACKEND_PORT=8080

# Frontend
FRONTEND_PORT=5173

# Weaviate (if using separate instance)
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_SCHEME=http
